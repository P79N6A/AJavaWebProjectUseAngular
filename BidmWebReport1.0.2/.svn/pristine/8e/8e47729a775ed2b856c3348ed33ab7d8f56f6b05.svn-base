import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { M1m2workwipComponent } from './m1m2workwip.component';

describe('M1m2workwipComponent', () => {
  let component: M1m2workwipComponent;
  let fixture: ComponentFixture<M1m2workwipComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ M1m2workwipComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(M1m2workwipComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
