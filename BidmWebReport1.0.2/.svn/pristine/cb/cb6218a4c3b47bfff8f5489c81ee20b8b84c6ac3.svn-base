import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CfwipmoveComponent } from './cfwipmove.component';

describe('CfwipmoveComponent', () => {
  let component: CfwipmoveComponent;
  let fixture: ComponentFixture<CfwipmoveComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CfwipmoveComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CfwipmoveComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
