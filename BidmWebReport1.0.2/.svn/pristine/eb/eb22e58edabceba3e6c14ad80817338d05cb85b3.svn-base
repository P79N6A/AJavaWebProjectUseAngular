import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CstglsComponent } from './cstgls.component';

describe('CstglsComponent', () => {
  let component: CstglsComponent;
  let fixture: ComponentFixture<CstglsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CstglsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CstglsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
