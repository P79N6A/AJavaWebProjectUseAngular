import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RatioAComponent } from './ratio-a.component';

describe('RatioAComponent', () => {
  let component: RatioAComponent;
  let fixture: ComponentFixture<RatioAComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RatioAComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RatioAComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
