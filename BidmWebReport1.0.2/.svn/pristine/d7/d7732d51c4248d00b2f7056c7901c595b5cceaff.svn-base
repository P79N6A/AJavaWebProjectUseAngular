import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ArraywipkanbanShishiComponent } from './arraywipkanban-shishi.component';

describe('ArraywipkanbanShishiComponent', () => {
  let component: ArraywipkanbanShishiComponent;
  let fixture: ComponentFixture<ArraywipkanbanShishiComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ArraywipkanbanShishiComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ArraywipkanbanShishiComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
