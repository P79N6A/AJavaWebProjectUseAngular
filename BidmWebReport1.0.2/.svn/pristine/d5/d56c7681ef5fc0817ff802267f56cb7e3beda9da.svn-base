import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ProwipdailyComponent } from './prowipdaily.component';

describe('ProwipdailyComponent', () => {
  let component: ProwipdailyComponent;
  let fixture: ComponentFixture<ProwipdailyComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ProwipdailyComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ProwipdailyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
