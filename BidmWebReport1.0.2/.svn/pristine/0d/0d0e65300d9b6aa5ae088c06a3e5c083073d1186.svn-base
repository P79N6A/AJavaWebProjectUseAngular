import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ArraymovegundongComponent } from './arraymovegundong.component';

describe('ArraymovegundongComponent', () => {
  let component: ArraymovegundongComponent;
  let fixture: ComponentFixture<ArraymovegundongComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ArraymovegundongComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ArraymovegundongComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
