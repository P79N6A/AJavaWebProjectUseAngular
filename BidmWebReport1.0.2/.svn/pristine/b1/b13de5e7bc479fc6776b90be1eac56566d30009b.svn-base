import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { GsproductComponent } from './gsproduct.component';

describe('GsproductComponent', () => {
  let component: GsproductComponent;
  let fixture: ComponentFixture<GsproductComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ GsproductComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(GsproductComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
