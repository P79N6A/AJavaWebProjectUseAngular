import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SortbankComponent } from './sortbank.component';

describe('SortbankComponent', () => {
  let component: SortbankComponent;
  let fixture: ComponentFixture<SortbankComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SortbankComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SortbankComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
