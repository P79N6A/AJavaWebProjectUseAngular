import { ToZeroPipePipe } from './to-zero-pipe.pipe';

describe('ToZeroPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ToZeroPipePipe();
    expect(pipe).toBeTruthy();
  });
});
