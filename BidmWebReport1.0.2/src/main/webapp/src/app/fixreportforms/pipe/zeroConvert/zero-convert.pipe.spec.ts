import { ZeroConvertPipe } from './zero-convert.pipe';

describe('ZeroConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new ZeroConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
