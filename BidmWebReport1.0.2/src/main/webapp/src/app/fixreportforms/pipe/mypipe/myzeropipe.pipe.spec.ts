import { MyzeropipePipe } from './myzeropipe.pipe';

describe('MyzeropipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyzeropipePipe();
    expect(pipe).toBeTruthy();
  });
});
