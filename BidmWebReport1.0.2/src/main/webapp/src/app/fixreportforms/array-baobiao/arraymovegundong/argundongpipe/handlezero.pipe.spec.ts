import { HandlezeroPipe } from './handlezero.pipe';

describe('HandlezeroPipe', () => {
  it('create an instance', () => {
    const pipe = new HandlezeroPipe();
    expect(pipe).toBeTruthy();
  });
});
